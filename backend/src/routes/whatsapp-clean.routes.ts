/**
 * WhatsApp Routes - Versione Pulita SOLO WPPConnect
 * Nessun riferimento a Evolution o SendApp
 * 
 * NOTA: Questo file è temporaneamente disabilitato perché dipende da whatsapp-unified.service
 * che non esiste. Usare invece le route in whatsapp.routes.ts
 * 
 * IL CODICE È STATO COMPLETAMENTE RIMOSSO PER EVITARE ERRORI TypeScript
 */

import { Router } from 'express';

const router = Router();

// Questo file è temporaneamente disabilitato
// Tutte le route WhatsApp sono gestite da whatsapp.routes.ts

export default router;

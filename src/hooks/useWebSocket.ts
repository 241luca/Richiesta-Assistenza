/**
 * useWebSocket Hook
 * Hook per gestire la connessione WebSocket - alias di useSocket
 */

import { useSocket } from './useSocket';

export const useWebSocket = useSocket;

export default useWebSocket;
// Questo file non è più necessario
// Il sistema usa già notification.service.ts e email.service.ts esistenti
// Vedere backend/src/services/notification.service.ts per il sistema completo

export const emailQueue = {
  add: async () => {
    console.log('Usa notification.service.ts invece');
  }
};